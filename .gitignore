# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Streamlit
.streamlit/secrets.toml

# OS
.DS_Store
Thumbs.db

# Downloads / temp media
*.mp3
*.mp4
*.part
*.webm
*.m4a

# Licensing secrets — NEVER commit these (would let anyone forge license keys)
secret.key
license_records.csv
*.dat

# AI / API credentials — NEVER commit (NVIDIA key, etc.)
nvidia.key
*.key
*api*key*.txt
*API*KEY*.txt
# Private analysis doc (contains another project's secret) — keep out of the repo
licensing_system_forensic_analysis.md

# Build artifacts (huge binaries / frozen output) — not for the repo
bin/
build/
dist/
runtime/
Output/
*.zip
*.spec
